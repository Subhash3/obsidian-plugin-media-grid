{
  "name": "obsidian-plugin-media-grid",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass:dev": "sass src/styles.scss build/styles.css -w",
    "esbuild:dev": "node esbuild.config.mjs",
    "dev": "concurrently \"pnpm run esbuild:dev\" \"pnpm run sass:dev\"",
    "esbuild:prod": "node esbuild.config.mjs production",
    "sass:prod": "sass src/styles.scss build/styles.css --no-source-map",
    "build:prod": "tsc && pnpm run esbuild:prod && pnpm run sass:prod",
    "clean": "rm -rf dist/ build/",
    "lint": "eslint .",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "release": "changeset publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.9.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "@changesets/cli": "^2.29.0",
    "concurrently": "^10.0.3",
    "esbuild": "^0.28.1",
    "eslint": "^10.6.0",
    "eslint-plugin-obsidianmd": "^0.3.0",
    "globals": "^17.7.0",
    "jiti": "^2.7.0",
    "obsidian": "^1.13.1",
    "sass": "^1.101.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.0"
  }
}