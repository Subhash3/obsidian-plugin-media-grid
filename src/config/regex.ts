export const regex = {
    media: /!\[\[([a-zA-Z0-9_-]+\.(\w+))(\|\d+)?\]\]/,
    cols: /cols\s*:\s*(\d+)/,
    gap: /gap\s*:\s*(\d+)/,
    gridContainerId: /grid-container-id\s*:\s*([a-zA-Z0-9_-]+)/
}